@model IEnumerable<FinanzasPersonalesWeb.Models.Monedas>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MonedaDescripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MonedaSimbolo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MonedaDescripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MonedaSimbolo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MonedaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.MonedaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MonedaId })
        </td>
    </tr>
}

</table>
