@model IEnumerable<FinanzasPersonalesWeb.Models.TiposIngresos>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TipoIngresoDescripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TipoIngresoDescripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TipoIngresoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TipoIngresoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TipoIngresoId })
        </td>
    </tr>
}

</table>
