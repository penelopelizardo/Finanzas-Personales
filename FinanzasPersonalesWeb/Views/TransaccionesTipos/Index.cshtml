@model IEnumerable<FinanzasPersonalesWeb.Models.TransaccionesTipos>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TranTiposDescripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TranTiposDescripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TranTiposId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TranTiposId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TranTiposId })
        </td>
    </tr>
}

</table>
